#!/bin/sh

set -e

# Create /var/db/repos and give the _apt user write permission
# if the directory doesn't exist already.
if [ ! -d /var/db/repos ]; then
	/usr/bin/mkdir -p /var/db/repos
	/usr/bin/chown _apt:_apt /var/db/repos
fi
